apiVersion: v1
kind: Service
metadata:
  name: crg9968-text-embedding-server-service
spec:
  selector:
    app: crg9968-text-embedding-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 50051
  type: LoadBalancer  # use LoadBalancer for external access
